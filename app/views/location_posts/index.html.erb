<% # 場所詳細画面 %>
<div class="profile-name">
  <%= @place_name %>
</div>

<div class="stats">
  <p>投稿数 <%= @microposts.count %></p>
  <p>総いいね数 <%= @microposts.joins(:likes).count %></p>
</div>

<% # 投稿一覧 %>
<div class="profile-microposts-container">
  <% @microposts.each do |micropost| %>
    <%= link_to (image_tag micropost.image), micropost_path(micropost), data: {"turbo" => false} %>
  <% end %>
</div>

<!--
<div class="row">

    <section class="user_info">
      <h1 class = "left">
      <%=@place_name %>
      </h1>
    </section>
    
    <div class="new-container">
      <p>投稿数: <%= @microposts.count %></p>
      <p>総いいね数: <%= @microposts.joins(:likes).count %></p>
    </div>

    <div class="row">
      <div class="microposts-container">
        <% @microposts.each do |micropost| %>
          <div class="microposts-column">
            <div class="profile-microposts">
              <%= link_to micropost_path(micropost) do %>
                <%= image_tag micropost.image, class: "micropost-image" if micropost.image.attached? %>
              <% end %>
            </div>
          </div>
          <% end %>
      </div>
    </div>

</div>
-->
