<% provide(:title, 'All users') %>

<%= will_paginate %>

<%= form_tag(users_path, method: :get) do %>
  <%= text_field_tag :search, params[:search] %>
  <%= image_submit_tag('search.png', class: 'submit-button') %>
<% end %>

<ul class="users">
  <%= render @users %>
</ul>

<%= will_paginate %>
